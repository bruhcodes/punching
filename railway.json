{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack pnpm@9.0.0 install --frozen-lockfile && corepack pnpm@9.0.0 --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "corepack pnpm@9.0.0 --filter @workspace/api-server run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
